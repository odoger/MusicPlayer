<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.example.emreoguz.singsong.MainActivity">


    <TextView
        android:text="Vokal Kaydetme ve Çalma"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/txtView"
        android:textSize="35sp"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:textAlignment="center"/>

    <ListView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/listMusics"
        android:layout_below="@id/txtView"
        android:entries="@array/musics"
        android:layout_centerVertical="true"/>

    <Button
        android:id="@+id/btnSoundStart"
        android:layout_width="64dp"
        android:layout_height="64dp"
        android:background="@drawable/plus"
        android:layout_margin="20dp"
        android:layout_alignParentBottom="true"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true"
        />
    <Button
        android:id="@+id/btnSoundStop"
        android:layout_width="64dp"
        android:layout_height="64dp"
        android:layout_toRightOf="@id/btnSoundStart"
        android:layout_toEndOf="@id/btnSoundStart"
        android:layout_alignParentBottom="true"
        android:background="@drawable/stop"
        android:layout_margin="20dp"/>
    <Button
        android:id="@+id/btnSoundPlay"
        android:layout_width="64dp"
        android:layout_height="64dp"
        android:layout_toEndOf="@id/btnSoundStop"
        android:layout_toRightOf="@id/btnSoundStop"
        android:layout_alignParentBottom="true"
        android:background="@drawable/play"
        android:layout_margin="20dp"/>
</RelativeLayout>

